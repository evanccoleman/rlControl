DATE: Spring 2026

==================================

OVERVIEW:

Get proper packages...
pip list --format=freeze > requirements.txt
pip install -r requirements.txt

What is implemented...
- Various Stable-Baselines3 agents
- Gymnasium environments
- POMDPWrapper class

Note:
- files from the cluster may have a 1-hour time difference

==================================

GOAL:

1. Does memory (LSTM) improve agent performance in continuous
control environments?

2. Does memory (LSTM) improve agent performance in POMDPs?

==================================

SUMMARY OF FILES AND DIRECTORIES:

old/ --> old stuff from Aug 2025 
        old agents,
        old walker files,
        old stuff (optuna optimizer, getgraphs.py, old dppg,
                   agents to be tested, and tuned agents)

notes/ --> personal notes

current/ --> code to work with and run right now 

param_files/ --> stores hyperparameters to try to train new agents on

outputs/ directory...
graphs/ --> stores graphs
runs/{agent}_{env}_{date}/ --> details and avgs an agent-env run
callbacks/{agent}_{env}_{date}/agent{i}_seed{i} --> from EvalCallback
checkpoints/{agent}_{env}_{date}/agent{i}_seed{i} --> best model and periodic model saves

